using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DialController : MonoBehaviour
{
    [SerializeField] public float Current_X_Rotation;
    
    void Start()
    {
        Current_X_Rotation = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
